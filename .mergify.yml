pull_request_rules:
  - name: VM1 auto-merge revoked pending-command trusted-actions backport
    conditions:
      - author = mergify[bot]
      - head ~= ^mergify/bp/vm1-revoked-actions-release-260914/pr-
      - base = vm1-revoked-actions-release-260914
    actions:
      merge:
        method: merge

  - name: VM1 bot-account caller credential boundary
    conditions:
      - title = VM1_BOT_SELECTION_PRIV_ESC
      - base = vm1-botselect-protected-260916a
    actions:
      merge:
        method: merge
        merge_bot_account: livastic-vm1-primary
